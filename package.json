{
  "name": "KinoFullStackFrontend",
  "version": "1.0.0",
  "description": "",
  "main": ".idea/movie/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build"
  },
  "private": true,
  "engines": {
    "node": "18.x"
  }
}